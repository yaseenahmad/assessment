import type { Config } from '@react-router/dev/config';

export default {
  appDirectory: 'src',
  ssr: false,
  // File-based routing will be handled automatically by the file structure in src/routes
} satisfies Config;